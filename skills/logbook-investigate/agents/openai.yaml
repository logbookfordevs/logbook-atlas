interface:
  display_name: "Investigate"
  short_description: "Resolve a bounded question with evidence"
  default_prompt: "Use $logbook-investigate to investigate this question and explain what the evidence supports."
policy:
  allow_implicit_invocation: true
