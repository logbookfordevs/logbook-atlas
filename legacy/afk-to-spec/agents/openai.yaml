interface:
  display_name: "AFK To Spec"
  short_description: "Create or normalize an agent-ready spec"
  default_prompt: "Create or normalize an agent-ready spec from this context."
policy:
  allow_implicit_invocation: false
